---
import { numberformatter } from '../util/formatters';

export interface Props {
  minutes: number
}

const { minutes } = Astro.props;
---

{minutes > 0 && <>({numberformatter.format(minutes)} minutter)</>}
